---
description: LeetCode 刷题仓库（TypeScript）题解与目录规范
alwaysApply: true
---

# LeetCode 刷题规范

## 语言与表达

- 说明、注释、README **必须中文**。
- **Git 提交说明（commit message）必须中文**，禁止纯英文或无意义短词（如 `update`、`fix`、`add`）。
- 提交说明写清「为什么 / 做了什么」，1～2 句即可，例如：`优化 tsconfig，改用 bundler 解析并固定工作区 TypeScript 版本`。
- 代码标识符保持英文，不使用拼音命名。

## 目录与命名

- 题解放在 `solutions/<分类>/`，统一 TypeScript。
- 文件名：`{英文短横线题名}.ts`（不含题号），例如 `two-sum.ts`。
- 公共能力从 `@/types`、`@/heap` 引入，禁止重复造轮子。
- 根目录不放零散题解文件。

## 题解质量

- 文件头写清：题意、示例、思路、复杂度；**标题不含题号**。
- 优先主流正确解法，代码简洁可推导。
- 底部保留可运行的 `console.log` 示例。
- README 仅维护目录结构，不维护题号/难度索引。
