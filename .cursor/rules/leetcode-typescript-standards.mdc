---
description: LeetCode 刷题仓库通用规范（原则式）
alwaysApply: true
---

# LeetCode 刷题规范

规则只约定稳定原则；新增分类或公共模块时不必改本文件。

## 语言

- 说明、注释、README、Git 提交说明用中文。
- 提交说明写清意图，禁止无信息英文（如 `update` / `fix` / `add`）。
- 代码标识符用英文，禁止拼音。

## 结构

- 题解：`solutions/<算法分类>/`；无合适目录则新建（英文短横线）。
- 文件名：英文短横线题名，不含题号。
- 公共代码：`src/` + `@/` 引入；能复用就不复制。

## 题解写法

- 文件头：中文题名（无题号）+ 题意 + 示例 + 思路 + 复杂度。
- 优先主流正确解法；底部保留 `console.log`。
- 已有同名题则升级，不重复建文件。

## 命令

- `npm run start -- <关键字>`
- `npm run typecheck`
