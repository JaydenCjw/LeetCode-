---
description: 资深游戏开发（TypeScript / Node.js / HTML5）全局编码规范
alwaysApply: true
---

# 游戏开发通用规范（TypeScript / Node.js / HTML5）

## 语言与沟通规范（新增）

- 默认使用中文进行说明、评审反馈、注释解释与实现思路描述。
- 在不影响代码可读性的前提下，文档与提交说明优先中文表达。
- 代码标识符（变量名、函数名、类型名）保持英文技术命名，不使用拼音命名。
- 面向用户或团队的说明优先中文；仅在协议字段、标准术语、API 必要处保留英文原词。

## 代码风格

- 代码风格追求简洁、技术化、可推导，不写冗余“修饰性”实现。
- 函数尽量小而单一职责，优先组合而非深层嵌套。
- 避免隐式副作用，状态变更必须显式且局部可追踪。
- 对非法输入与不可能状态快速失败，并给出清晰错误语义。
- 仅优化热点路径，但代码必须保持可读、可分析、可剖析。

## 架构与结构

- 明确拆分领域逻辑、渲染层、IO/网络层、平台适配层。
- 游戏循环与高频路径关注分配成本，避免逐帧不必要对象创建。
- 通用数学/时间/状态转换优先无副作用工具模块。
- 网络、存储、引擎适配等边界层必须使用显式类型与接口。
- 保持模块聚焦，避免“巨石文件”和跨模块耦合蔓延。

## TypeScript 与 Node.js 规范

- 使用严格类型约束；除边界场景外避免 `any`。
- 共享状态优先 `readonly` 与不可变值对象。
- 有限状态和事件分支优先使用可辨识联合类型。
- 异步流程使用 `async/await` 并显式处理错误，禁止吞异常。
- Node.js 外部输入在进入核心逻辑前必须校验与规范化。

## 命名规范

- `camelCase`：变量、参数、函数、方法。
- `PascalCase`：类型、接口、类、枚举。
- `UPPER_SNAKE_CASE`：常量与不可变配置键。
- 布尔命名必须可判定：`isReady`、`hasTarget`、`canRetry`。
- 事件相关命名优先前缀：`on`、`handle`、`emit`、`dispatch`。

## 缩写规范

- 默认优先完整单词，仅在行业通用场景使用缩写。
- 允许常用缩写：`id`、`ui`、`fps`、`hp`、`mp`、`xp`、`pos`、`rot`、`vel`、`acc`、`dt`。
- 禁止随意发明项目私有缩写；首次使用非直观缩写需就近说明。
- 同一缩写在全仓库语义必须一致（例如 `dt` 始终表示 delta time）。
- 同一作用域避免同概念混用全称与缩写（如 `position` 与 `pos` 并存）。

## HTML5 游戏前端规范

- 逻辑更新（update）与渲染（render）阶段保持清晰分离。
- 禁止在帧线程执行高成本同步阻塞操作。
- 复用 DOM/canvas 查询结果，避免无意义重排与布局抖动。
- 坐标命名使用显式语义：`worldX`、`screenX`、`uiX`。
- 输入映射与归一化统一管理，避免键鼠逻辑多处重复。

## AI 生成代码要求

- 生成代码优先可读性，其次再优化热点路径性能。
- 仅在逻辑不直观或性能敏感处添加简短注释。
- 对外 API 保持清晰稳定，避免大范围无必要破坏性重命名。
- 命名不确定时优先用完整英文语义，再考虑允许缩写。
